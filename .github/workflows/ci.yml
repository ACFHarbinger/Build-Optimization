name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --group dev
        working-directory: middleware
      - run: uv run ruff check .
        working-directory: middleware
      - run: uv run mypy src
        working-directory: middleware

  test-python:
    runs-on: ubuntu-latest
    needs: [lint-python, build-backend]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      # Pinned to match build-backend's Pixi environment (python = "3.11.*"
      # in backend/pixi.toml) so the downloaded .so's ABI tag
      # (cpython-311-...) matches the interpreter running pytest.
      - run: uv sync --python 3.11 --group dev
        working-directory: middleware
      - uses: actions/download-artifact@v4
        with:
          name: build-optimizer-backend-so
          path: backend
      - run: uv run pytest tests -v --cov=src --cov-report=xml
        working-directory: middleware
      - uses: codecov/codecov-action@v4
        with:
          files: middleware/coverage.xml
          fail_ci_if_error: false

  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: prefix-dev/setup-pixi@v0.8.1
        with:
          manifest-path: backend/pixi.toml
      - run: pixi run --manifest-path backend/pixi.toml build
      - run: pixi run --manifest-path backend/pixi.toml test
      - run: pixi run --manifest-path backend/pixi.toml install
      - uses: actions/upload-artifact@v4
        with:
          name: build-optimizer-backend-so
          path: backend/*.so

  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install --workspaces
      - run: npm run test-frontend
      - run: npm run test-extension
      - run: cd frontend/src-tauri && cargo check
